# Test and lint tooling. Not required to run the system.
# Install: python -m pip install -r requirements-dev.txt
-r requirements.txt

pytest==8.3.4
pytest-asyncio==0.25.0
pytest-cov==6.0.0
black==24.10.0
flake8==7.1.1
